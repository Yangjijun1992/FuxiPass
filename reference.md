# 安全密码管家 APP 软件需求规格文档 (PRD)

> **版本**：V2.0（安全加固版）
> **修订说明**：本版基于 V1.0 安全评审结论修订。核心变更包括——
> 1. 删除「输错 10 次清空本地缓存」反模式，改为**密码提示词 + 邮箱/短信验证恢复机制**；
> 2. 新增**密钥架构 / 数据生命周期 / 隐私合规 / 验收标准**四个章节；
> 3. 校正字段分级（备注含密保答案，提升为高敏感）、修正「打印可取消打码」、收敛自动抓取高危权限；
> 4. 为所有安全与功能模块给出**可测试的验收标准**。

---

## 0. 文档信息

| 项 | 内容 |
| :-- | :-- |
| 项目名称 | 安全密码管家（暂定名） |
| 文档版本 | V2.0 |
| 核心定位 | **高安全级别、零知识边界、灾难可恢复**的个人账户与密码管理工具 |
| 安全原则 | 零知识架构、本地强加密、**主密码可恢复（不可逆失不可取）**、高敏感操作二次验证、权限最小化、极简一键交互 |

> ⚠️ **零知识边界的明确定义**：服务端（若有云备份/恢复）**永不存储、永不观测**主密码、主密码派生密钥（KEK）及任何明文凭证。服务端可存储的最小单位是：
> - 用**独立高熵恢复密钥（Recovery Key）**加密的「数据加密密钥（DEK）包裹」；
> - 经邮箱/短信验证后的一次性下发权限。
> 服务端**无法单独**用其持有的数据解密明文，**必须**依赖用户提供的恢复密钥。二者分离即构成「可恢复但不泄密」的零知识边界。

---

## 1. 项目概述与设计原则

本应用旨在帮助用户在零知识、强加密前提下安全便捷地管理账号密码及支付凭证，并**在用户忘记主密码时不丢失任何原始数据**。

### 1.1 设计原则（新增/修订）

1. **零知识 + 可恢复**：本地加密保证服务端不可见；同时提供**密码提示词、恢复码、邮箱/短信验证**等多层次恢复通道，避免主密码遗忘导致数据永久丢失。
2. **最小权限**：涉及读取跨应用/网页输入（自动抓取、AutoFill）时，权限严格最小化、默认关闭、逐项授权并明示信任边界。
3. **防爆破不毁数据**：连续输错只做**指数退避锁定**，**任何自动触发都不允许删除或清空用户数据**；数据销毁必须是用户**主动、显式、二次确认**的操作。
4. **分级管控**：常规信息快速获取；高敏感信息（二级密码、支付密码、密钥/私钥、含密保答案的备注）必须二次验证。
5. **脱敏始终生效**：打印/导出的打码**默认不可一键关闭**，取消打码需**二次确认 + 强警示**。
6. **剪贴板最小暴露**：高敏感字段**默认不写系统剪贴板**，改为受控交互；普通字段复制仍提供 30 秒自清空。

---

## 2. 安全架构（新增章节，V1.0 缺失）

> V1.0 只写「AES-256-GCM / Argon2id」，但**未定义密钥体系如何组装**。本节补全为可实现的密码学模型。

### 2.1 密钥体系（分层加密，Key Hierarchy）

| 密钥 | 生成方式 | 用途 | 存储位置 |
| :-- | :-- | :-- | :-- |
| **KEK（Key Encryption Key）** | 主密码 → Argon2id（内存大、迭代高、随机盐）派生 | 用于加密/解包 DEK | **不存储**，每次由主密码即时派生 |
| **DEK（Data Encryption Key，256-bit）** | 首次初始化时用 CSPRNG 随机生成 | 用 AES-256-GCM 加密全部账号数据 | 以「KEK 包裹的密文」存于本地加密库 |
| **Recovery Key（恢复密钥）** | 首次设置主密码时由 CSPRNG 生成高熵随机串（≥ 128 bit），编码为分组显示的人类可读码 | 端到端加密 DEK 包裹，用于忘记主密码时重置 | 仅用户持有（导出/打印/备份）；服务端只存其加密后的 DEK 包裹 |
| **Account Key（设备密钥）** | 初始化时生成 | 存于 OS Keystore/Keychain（受生物识别 & TEE 保护），用于**快捷解锁会话** | OS 硬件安全存储（Android Keystore / iOS Keychain） |

**设计要点**：
- 主密码**永不直接加密数据**，而是派生 KEK 去包 DEK。修改主密码只需**重包 DEK**，无需重加密全量数据（性能与安全双赢）。
- 生物识别**不替代主密码**：仅用于解锁 `Account Key` 开启一个**短期会话密钥**。系统强制 **每 24 小时（可配置，默认 24h）要求主密码**重新解锁一次，避免长期退化为单因素。
- 高敏感字段可选用 **FDEK（Field Data Encryption Key）** 单独加密，使「二次验证前」这些字段密文在库中即不可用，实现字段级权限而非仅 UI 拦截。

### 2.2 恢复 / 主密码重置流程（用户明确诉求，V1.0 缺失）

> **原则：主密码多次输错，绝不删除或清空任何原始数据。**

**解锁失败处理**：
- 连续输错主密码 **1~4 次**：仅提示「密码错误，剩余 N 次尝试」。
- 连续输错 **第 5 次**：进入 5 分钟指数退避锁定，并弹出选项：
  - 「**显示密码提示词**」（若用户已设置）；
  - 「**通过邮箱/手机号找回**」（若已绑定，见下）。
- **退避策略**：每次错误尝试后锁定时间翻倍（如 5m → 10m → 20m …，上限 30 分钟）。
- **删除数据选项**：仅在「忘记所有恢复手段」时**主动向用户提供**，且必须：输入确认短语 → 要求再次输入当前主密码（若仍记得）/恢复密钥 → 二次确认弹窗 → 才执行。**严禁自动触发。**

**恢复通道（三个可选层次，用户可分别启用）**：

1. **密码提示词（Password Hint）**——纯本地、可选
   - 设置主密码时可配置一条与密码**无强关联**的提示文本（系统会提示「请勿直接写入密码」）。
   - 仅本地存储，不上传；在解锁失败时显示。

2. **本地恢复码（Recovery Key）**——零网络、可选
   - 首次初始化时生成，用户可**导出/打印/备份**（建议离线安全存放）。
   - 使用：忘记主密码 → 选择「用恢复码重置」→ 输入恢复码 → 客户端用恢复码解开 DEK 包裹 → 引导设置**新主密码**（重新包裹 DEK）。

3. **邮箱 / 短信验证找回**——可选，需绑定联系人
   - 用户绑定邮箱/手机号（仅用于找回，不用于任何营销）。
   - 忘记主密码 → 验证码校验（邮箱/短信）→ 服务端只负责**下发「恢复码包裹」**（即用恢复码加密的 DEK），**服务端本身持有任何可解密恢复明文的密钥**。
   - 客户端拿到包裹后，需再输入用户的 Recovery Key 才能解包——即「**验证码（第二因素）+ 恢复码（真正密钥）**」两把钥匙缺一不可。
   - **零知识边界**：即使攻击者通过短信/邮箱拿到验证码，若没有用户的恢复码，仍无法解包；服务端日志中不落恢复码明文。

> **验收第 1 条（核心安全合规）**：全流程任一途径下，服务端、审计日志、调试日志中**均不得出现**主密码、KEK、Recovery Key 明文。

### 2.3 密钥轮换与数据迁移

- 修改主密码 → 仅重包 DEK，不重加密全量；
- DEK 轮换（可选）→ 触发全量重加密，需主密码验证；
- 换机迁移：使用「加密备份文件 + Recovery Key」在目标设备解包，**无需云同步**；
- 多设备（如支持）→ 仅同步「DEK 包裹 + Recovery Key 加密的密文」，服务端不可读取。

---

## 3. 功能需求详细说明

### 3.1 账号与多字段结构设计（修订：校正分级）

每个账号为一张独立「加密卡片」，含结构化字段。**安全级别经重新评估**：

| 字段名称 | 字段类型 | 示例 / 说明 | 安全级别 |
| :-- | :-- | :-- | :-- |
| 应用/网站名称 | 文本 | 淘宝、建设银行 APP、中国国家图书馆 | 普通 |
| 网址 / 应用包名 | 文本/URL | `https://www.taobao.com`，一键跳转 | 普通 |
| 账号 / 用户名 | 文本 | 手机号、邮箱、身份证号、自定义用户名 | **中（可能含个人敏感信息）** |
| 登录密码 | 字符串 | `666666XXX@QQQ` | 高 |
| 二级密码 | 字符串 | 交易密码、独立安全码 | **极高（需二次验证）** |
| 支付密码 | 数字/字符串 | `888444`（银行卡支付密码） | **极高（需二次验证）** |
| 密钥 / API Key | 字符串 | 谷歌验证器密钥、区块链私钥、API 密钥 | **极高（需二次验证，私钥需差异化导出）** |
| **重要度 / 标签** | 枚举/分类 | 常用 / 金融银行 / 低频备用 / 社交 | 普通 |
| **备注说明** | 文本 | 密保答案、预留手机号、注册时间等 | **高（密保答案为高价值凭证）** |

> **分级修正说明**：备注中常含「密保问题答案」，配合账号可被用于账户接管，故从「普通」上调至「高」，查看/复制需二次验证（除非用户手动标记为普通类备注）。账号字段可能为手机号/身份证，涉《个人信息保护法（PIPL）》，标注为「中」。

**字段校验规则（新增）**：
- 应用名、账号（至少其一）必填；网址需格式校验；
- 同一平台可存多账号（如淘宝 A/B），以「平台 + 自定义标签」去重标识；
- 高敏感字段（支付密码/二级密码/密钥）值需二次确认防误输。

### 3.2 新登录自动抓取与保存（高权限，收敛设计）

> ⚠️ **权限风险收敛（V1.0 最大安全隐患之一）**：跨应用/网页读取输入在 Android 依赖 Accessibility 无障碍、浏览器依赖「读取所有网页输入」，这类权限本身与恶意软件同类。必须**最小化 + 默认关闭 + 明示。**

- **3.2.1 登录页识别**：检测 URL/页面含「登录/注册/Sign In/Login」特征且存在密码表单时触发**候选**。
- **3.2.2 授权与信任边界（新增，关键）**
  - **主开关**存在，但**默认关闭**；首次开启必须展示「权限说明 + 隐私政策」，用户逐项同意后才激活。
  - **显式授权域**：用户**逐个添加**允许监控的域名/应用，而非全量监听；未列入的站点一律不触发。
  - **黑名单**：可将任一站点/应用移出授权域（黑名单优先级高于白名单）。
  - **采集方式约束**：只读取「该表单提交时的账号密码字段」，且这些值**仅驻留内存用于临时预览，不落盘、不写日志**。
- **3.2.3 弹窗确认与自动保存（明文预览修正）**
  - 登录成功后弹出悬浮卡片：「检测到新登录，是否保存？」
  - 卡片默认**密码脱敏显示**（`••••`），用户需点「显示」才展开（展开时该弹窗套用防截图保护）；
  - 提供：应用名（可改）、账号、登录密码预览；「确认保存」后写入加密库。

### 3.3 历史数据智能导入（QQ 记事本解析）

- **3.3.1** 粘贴非结构化文本文段，自动解析 `[平台] [账号] [密码] [备注]`。
- **3.3.2** 表格化预览 + 手动微调/删除/补全；**校验**：缺必填字段标记红色，重复账号提示合并；导入前需主密码验证。
- **边界（新增）**：解析出的明文在预览阶段同样走防截图；导入完成后立即清空粘贴缓冲与临时变量。

### 3.4 权限验证与分级安全控制（修订）

- **3.4.1 主密码与根解锁**：打开 APP / 解密数据库需主密码；支持生物识别**快捷会话解锁**（见 2.1，24h 强制主密码）。生物识别不上传。
- **3.4.2 极高敏感字段二次身份验证**：查看明文/复制/编辑/导出 `二级密码`、`支付密码`、`密钥/API Key`、`高敏感备注` 时，强制**重新身份验证**（生物识别或主密码），验证会话默认 **2 分钟**内可复用（可配置），超时后再次操作需重新验证。
- **3.4.3 自动锁屏与防暴力破解（修订：禁毁数据 + 可恢复 + 退避）**
  - 超时锁定：切后台 > 30 秒或屏灭即锁；
  - **防爆破**：连续输错 5 次锁 5 分钟，之后**指数退避**（5m→10m→20m→上限 30m）；
  - **绝不自动清空/删除**；错误次数较多时引导「显示密码提示」或「邮箱/短信找回」；
  - 清除/重置数据 = **主动 + 二次确认**操作，且需证明用户身份（输入确认短语并验证）。

### 3.5 查询、分项复制与打印输出（修订）

- **3.5.1 快速检索**：拼音首字母、网站名、账号名模糊搜索（如 `12306`、`JSYH`）。
- **3.5.2 分项一键复制**：账号卡片每个字段独立复制按钮。
- **3.5.3 剪贴板策略（修订：默认不写入）**
  - 普通字段（网址/账号）：复制后 30 秒自动清空，多次复制重置计时；
  - **高敏感字段（密码/二级密码/支付密码/密钥）**：**默认不写系统剪贴板**，改为提供「安全输入面板 + 长按粘贴到目标 App」或「跳转系统安全键盘粘贴」；若用户选择传统复制，则复制后立即清空（可配置），并强警示剪贴板风险。
- **3.5.4 打印与导出（修订：脱敏不可一键关闭）**
  - 打印/导出前必须二次验证；
  - **默认脱敏打码**（如 `88****`）；**取消打码需二次确认 + 高风险警示**，且该操作在打印/导出会话内需再次验证，**非永久默认关闭**；
  - 导出为**加密备份文件**（用 Recovery Key/独立导出口令加密），提供「导入恢复」对应流程。

### 3.6 系统级自动填充（Auto-fill）

- 对接 Android AutoFill Service / iOS Keychain AutoFill；
- 在授权域内调用时，通过系统安全通道回填账号密码；**不抓取、不记录**未授权站点；
- 涉及跨 APP 读取整机输入时，默认为「用户主动选择填充」，而非后台监听。

---

## 4. 非功能与安全需求（修订表）

| 安全维度 | 技术标准 / 实现方案 | 目的 / 防御场景 |
| :-- | :-- | :-- |
| 数据库加密 | AES-256-GCM / SQLCipher，DEK 用 KEK 包裹存储 | 保护本地文件，手机丢失/拷贝也无法破解 |
| 密钥派生 | Argon2id（高内存/迭代/随机盐），内存与时长可配置 | 提升对主密码的暴力破解成本 |
| **密钥管理** | 分层（KEK/DEK/Recovery Key/Account Key），生物识别仅解锁短期会话 | 避免退化为单因素，支持主密码可恢复 |
| **恢复/重置** | 密码提示 + 恢复码 + 邮箱/短信验证（两把钥匙原则） | 忘记主密码不丢数据，且不破坏零知识边界 |
| 界面防截图/录屏 | FLAG_SECURE（Android）；iOS 用前台监听/证书越狱检测（能力差异化说明） | 防明文在截图/录屏/后台预览泄露 |
| 内存安全 | 敏感值用不可变专用容器 + 退出即置空引用；**明确说明托管语言无法可靠物理清零** | 降低敏感值长期驻留内存风险 |
| 剪贴板保护 | 普通字段 30 秒自清空；高敏感字段默认不写剪贴板 | 防第三方恶意应用/系统剪贴板历史器窃取 |

---

## 5. 数据生命周期（新增章节）

| 阶段 | 策略 |
| :-- | :-- |
| **创建** | 录入经校验；高敏感字段二次确认；写库即加密 |
| **读取** | 主密码会话解锁；高敏感字段需二次验证；防截图 |
| **修改** | 高敏感字段修改需二次验证；记录可选审计日志（何时改、改哪类，不落明文） |
| **删除** | 单卡删除支持「撤销（软删→观察期→安全擦除）」；观察期默认可配置 |
| **全量清除** | 仅为主动 + 二次确认操作；清除前允许导出备份 |
| **备份/恢复** | 加密备份文件（Recovery Key 包裹）；提供完整导入恢复流程；定期备份提醒 |
| **换机/迁移** | 使用备份 + 恢复码解包，无需云同步（或仅同步包裹密文） |

---

## 6. 界面结构与交互设计框架（修订示意图）

```text
==================================================
[主界面]
 ├── 🔍 搜索框（名称/拼音/账号）
 ├── ⚙️ 自动抓取状态: [已关闭]（默认）/[开启] + 「管理授权域」
 ├── 📁 分类标签: [全部] [常用] [金融支付] [低频备用]
 └── ➕ 快捷操作: [新增账号] | [导入QQ记事本文本]
==================================================
[账号详情页]
 ├── 应用/网站: 中国国家图书馆 [跳转] [复制]
 ├── 登录账号:  zhangsan_123 [复制账号]
 ├── 登录密码:  •••••••••••• [👁显示] [复制(受控)]
 ├── 备注信息:  密保答案：xxx（高敏感） [🔑解锁查看] [复制]
 ├───────────────────────────────────────────────
 │  🔒 支付密码: •••••• [🔑解锁查看] [复制(受控)]
 │  🔒 二级密码: •••••••• [🔑解锁查看] [复制(受控)]
 └───────────────────────────────────────────────
 ├── 🖨️ [安全打印/导出] (二次验证 + 默认脱敏，取消打码需二次确认)
 └── 🗑️ [删除此卡片] (支持撤销 / 观察期)
==================================================

[恢复/找回界面]（解锁失败时出现）
 ├── 🪪 显示密码提示词
 ├── 📧 通过邮箱/短信找回（验证码）
 └── 🔑 恢复码重置主密码
```

---

## 7. 隐私与合规（新增章节）

- **个人信息保护法（PIPL）**：账号/备注可能含手机号、身份证、密保答案等个人信息；需明示收集范围、提供删除权/导出权/更正权。
- **极简数据留存**：服务端仅存「加密包裹 + 找回用绑定标识」，不含任何明文；不用于营销，不向第三方共享。
- **权限最小化**：抓取/AutoFill 等敏感权限需逐项授权 + 可随时撤销；卸载即清除本地授权清单。
- **合规可审计（可选）**：高敏感字段的查看/复制/导出记录「操作时间 + 字段类别」（不落明文）审计日志，便于用户自检是否有异常访问。

---

## 8. 验收标准（新增章节 —— 每个模块给出可测试的通过条件）

> 以下为**可执行验收项**，开发与 QA 据此判定是否交付。按模块/版本分批验收。

### 8.1 密钥架构与加密（Phase 1）
- [ ] 全量数据以 AES-256-GCM 加密，密文中**未出现**任何明文密码片段；SQLCipher 页面加密开启。
- [ ] 主密码经 Argon2id 派生（参数随文档给出并可配置），算法与参数在代码中可审计。
- [ ] 修改主密码仅重包 DEK，**不重加密全量**；耗时< 阈值且数据完整。
- [ ] 生物识别解锁的是短期会话（Account Key），并强制每 24h 或配置时长重新要求主密码。
- [ ] 服务端（含日志）中**检索不到**主密码/KEK/Recovery Key 明文。

### 8.2 恢复/重置（Phase 1/2，核心诉求）
- [ ] 连续输错 5 次进入退避锁定；**任何错误次数下**均**不删除/清空**原始数据。
- [ ] 解锁失败界面提供：显示提示词、邮箱/短信找回、恢复码重置，三个入口。
- [ ] 邮箱/短信验证通过后，只能拿到「恢复码包裹」；**无恢复码无法解包**出明文。
- [ ] 使用恢复码可成功重置新主密码，且重置后旧加密数据完整可读。
- [ ] 全量清除仅在主动 + 二次确认（含确认短语）后执行，执行前提供备份导出。

### 8.3 分级鉴权与高敏感字段
- [ ] 查看/复制/编辑/导出 `二级密码/支付密码/密钥/高敏感备注` 均触发二次验证。
- [ ] 二次验证会话 2 分钟（可配置）后自动失效，需重新验证。
- [ ] 未验证前，上述字段密文在 UI 与查询结果中**均不可见**（字段级权限）。

### 8.4 自动抓取 / AutoFill（Phase 3）
- [ ] 抓取**默认关闭**；开启需逐项同意权限说明。
- [ ] 仅对「授权域」内站点触发；未授权站点**不**抓取、**不**记录。
- [ ] 黑名单站点优先级高于白名单，且生效。
- [ ] 捕获的账号密码仅驻留内存、不落盘、不写日志；预览默认脱敏。

### 8.5 剪贴板 / 打印 / 导出
- [ ] 普通字段复制后 30 秒自动清空、可配置。
- [ ] 高敏感字段**默认不写入**系统剪贴板（走安全输入面板或受控复制）。
- [ ] 打印/导出二次验证；**默认打码**；取消打码需二次确认 + 警示，且非永久默认关闭。
- [ ] 导出文件可跨设备「导入恢复」成功。

### 8.6 隐私合规（Phase 4）
- [ ] 应用配置页可查看/导出/删除个人数据。
- [ ] 每次高敏感操作写入审计日志（不含明文），可查询。
- [ ] 卸载后本地授权清单、临时文件被清除。

---

## 9. 开发实施路线图（修订）

```text
────────────────────────────────────
Phase 1：安全底座与本地核心
 搭建密钥架构（KEK/DEK/Recovery Key/Account Key）。
 实现 Argon2id 派生、AES-256-GCM 加密数据库。
 主密码设置 + 生物识别快捷会话 + 24h 强制主密码。
 账号卡片增删改查、分项复制、剪贴板策略。
 密码提示词与本地恢复码（V2.0 新增、核心）。

Phase 2：分级鉴权 / 恢复 / 旧数据导入
 二级密码、支付密码、密钥查看/复制/打印二次验证。
 邮箱/短信验证找回 + 恢复码重置主密码（V2.0 新增）。
 指数退避防爆破（仅锁定、绝不毁数据）。
 「QQ 记事本」非结构化解析与批量导入。

Phase 3：智能抓取与系统集成
 登录页识别与抓取（默认关闭 + 授权域 + 信任边界）。
 Android / iOS AutoFill（最小权限、主动填充）。
 加密备份导出与「导入恢复」完整流程（V2.0 新增）。

Phase 4：安全加固与审计
 防截图/录屏、内存清空策略、剪贴板体系强化。
 高敏感字段审计日志、PIPL 合规、数据生命周期。
 代码级安全审计、内存泄露排查、渗透测试。
────────────────────────────────────
```

---

## 10. 缺口（V1.0）与解决方案对照表（评审结论落地）

> 以下将第 1 轮评审发现的全部缺口，逐一映射到本版 V2.0 的解决位置。

| # | 原缺口（V1.0） | 严重度 | 解决方案（V2.0 位置） |
| :-- | :-- | :-- | :-- |
| 1 | 主密码遗忘即永久丢数据，无恢复机制 | 🔴 高 | 新增 2.2：密码提示词 / 本地恢复码 / 邮箱+短信验证；8.2 验收 |
| 2 | 「输错 10 次清空缓存」= 可被恶意摧毁数据 | 🔴 致命 | 3.4.3：改为指数退避锁定，**绝不自动清空**；清除需主动+二次确认 |
| 3 | 自动抓取读取跨应用/网页输入，权限等同恶意软件 | 🔴 高 | 3.2.2：默认关闭 + 授权域 + 黑名单 + 权限说明；8.4 验收 |
| 4 | 剪贴板系统级共享面被低估（含系统历史器缓存） | 🟠 高 | 3.5.3：高敏感字段**默认不写剪贴板**，改受控复制 |
| 5 | 「可取消打码」使脱敏形同虚设 | 🟠 高 | 3.5.4 / 8.5：默认打码，取消需二次确认+警示，非永久关闭 |
| 6 | 生物识别退化为单因素 | 🟠 高 | 2.1：生物只解锁短期会话，24h 强制主密码；8.1 验收 |
| 7 | 无密钥体系组装方案 | 🟠 高 | 2.1：KEK/DEK/Recovery Key/Account Key 分层；8.1 验收 |
| 8 | 备注（含密保答案）标为「普通」被低估 | 🟡 中 | 3.1：备注上调为「高」，含密保答案需二次验证 |
| 9 | 「内存即擦除」在托管语言不成立（过度承诺） | 🟡 中 | 4：改为专用容器 + 退出置空引用 + 差异化说明 |
| 10 | 防录屏仅 Android，iOS 无对等 | 🟡 中 | 4：差异化能力说明（FLAG_SECURE 仅 Android，iOS 用前台机制） |
| 11 | 无数据备份/迁移/多设备策略 | 🟡 中 | 2.3 + 5：备份/恢复/换机迁移流程；8.5 验收 |
| 12 | 无主密码强度策略 | 🟡 中 | 2.1（新增约束）+ 8.1：设定最小长度/复杂度，防离线爆破 |
| 13 | 无字段校验/重复账号/多账号规则 | 🟡 中 | 3.1：字段校验规则 + 平台多账号 + 去重 |
| 14 | 无权限模型 / 平台范围澄清 | 🟡 中 | 7：隐私与权限最小化；路线图按 Phase 拆解 |
| 15 | 无验收标准 | 🟡 中 | 第 8 章：全模块可测试验收项 |
| 16 | 抓取弹窗直接明文展示密码 | 🟡 中 | 3.2.3：默认脱敏 + 防截图 |
| 17 | 二次验证无失败/超时策略 | 🟡 中 | 3.4.2：验证会话 2 分钟 + 失败回退 |
| 18 | 私钥/TOTP 密钥需差异化处理 | 🟡 中 | 3.1 字段说明 + 3.4.2：私钥单独导出方案 |

> **验收第 0 条（总纲）**：V2.0 全部章节通过 8.1–8.6 验收项方能视为交付；任何一项涉及「**自动删除/清空用户数据**」的自动化路径均视为**验收不通过**。

---

*文档结束 — 安全密码管家 V2.0（安全加固版）*
